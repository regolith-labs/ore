/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { ORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CHECKPOINT_DISCRIMINATOR = 2;

export function getCheckpointDiscriminatorBytes() {
  return getU8Encoder().encode(CHECKPOINT_DISCRIMINATOR);
}

export type CheckpointInstruction<
  TProgram extends string = typeof ORE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountBoard extends string | AccountMeta<string> = string,
  TAccountMiner extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountBoard extends string
        ? ReadonlyAccount<TAccountBoard>
        : TAccountBoard,
      TAccountMiner extends string
        ? WritableAccount<TAccountMiner>
        : TAccountMiner,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CheckpointInstructionData = { discriminator: number };

export type CheckpointInstructionDataArgs = {};

export function getCheckpointInstructionDataEncoder(): FixedSizeEncoder<CheckpointInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: CHECKPOINT_DISCRIMINATOR }),
  );
}

export function getCheckpointInstructionDataDecoder(): FixedSizeDecoder<CheckpointInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getCheckpointInstructionDataCodec(): FixedSizeCodec<
  CheckpointInstructionDataArgs,
  CheckpointInstructionData
> {
  return combineCodec(
    getCheckpointInstructionDataEncoder(),
    getCheckpointInstructionDataDecoder(),
  );
}

export type CheckpointInput<
  TAccountSigner extends string = string,
  TAccountBoard extends string = string,
  TAccountMiner extends string = string,
  TAccountRound extends string = string,
  TAccountTreasury extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  board: Address<TAccountBoard>;
  miner: Address<TAccountMiner>;
  round: Address<TAccountRound>;
  treasury: Address<TAccountTreasury>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCheckpointInstruction<
  TAccountSigner extends string,
  TAccountBoard extends string,
  TAccountMiner extends string,
  TAccountRound extends string,
  TAccountTreasury extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORE_PROGRAM_ADDRESS,
>(
  input: CheckpointInput<
    TAccountSigner,
    TAccountBoard,
    TAccountMiner,
    TAccountRound,
    TAccountTreasury,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CheckpointInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountBoard,
  TAccountMiner,
  TAccountRound,
  TAccountTreasury,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    board: { value: input.board ?? null, isWritable: false },
    miner: { value: input.miner ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.board),
      getAccountMeta(accounts.miner),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.treasury),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCheckpointInstructionDataEncoder().encode({}),
    programAddress,
  } as CheckpointInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountBoard,
    TAccountMiner,
    TAccountRound,
    TAccountTreasury,
    TAccountSystemProgram
  >);
}

export type ParsedCheckpointInstruction<
  TProgram extends string = typeof ORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    board: TAccountMetas[1];
    miner: TAccountMetas[2];
    round: TAccountMetas[3];
    treasury: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: CheckpointInstructionData;
};

export function parseCheckpointInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCheckpointInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      board: getNextAccount(),
      miner: getNextAccount(),
      round: getNextAccount(),
      treasury: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCheckpointInstructionDataDecoder().decode(instruction.data),
  };
}
