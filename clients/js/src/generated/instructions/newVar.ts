/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { ORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const NEW_VAR_DISCRIMINATOR = 17;

export function getNewVarDiscriminatorBytes() {
  return getU8Encoder().encode(NEW_VAR_DISCRIMINATOR);
}

export type NewVarInstruction<
  TProgram extends string = typeof ORE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountBoard extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountVar extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountEntropyProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountBoard extends string
        ? WritableAccount<TAccountBoard>
        : TAccountBoard,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountProvider extends string
        ? ReadonlyAccount<TAccountProvider>
        : TAccountProvider,
      TAccountVar extends string ? WritableAccount<TAccountVar> : TAccountVar,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountEntropyProgram extends string
        ? ReadonlyAccount<TAccountEntropyProgram>
        : TAccountEntropyProgram,
      ...TRemainingAccounts,
    ]
  >;

export type NewVarInstructionData = {
  discriminator: number;
  id: bigint;
  commit: ReadonlyUint8Array;
  samples: bigint;
};

export type NewVarInstructionDataArgs = {
  id: number | bigint;
  commit: ReadonlyUint8Array;
  samples: number | bigint;
};

export function getNewVarInstructionDataEncoder(): FixedSizeEncoder<NewVarInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["id", getU64Encoder()],
      ["commit", fixEncoderSize(getBytesEncoder(), 32)],
      ["samples", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: NEW_VAR_DISCRIMINATOR }),
  );
}

export function getNewVarInstructionDataDecoder(): FixedSizeDecoder<NewVarInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["id", getU64Decoder()],
    ["commit", fixDecoderSize(getBytesDecoder(), 32)],
    ["samples", getU64Decoder()],
  ]);
}

export function getNewVarInstructionDataCodec(): FixedSizeCodec<
  NewVarInstructionDataArgs,
  NewVarInstructionData
> {
  return combineCodec(
    getNewVarInstructionDataEncoder(),
    getNewVarInstructionDataDecoder(),
  );
}

export type NewVarInput<
  TAccountSigner extends string = string,
  TAccountBoard extends string = string,
  TAccountConfig extends string = string,
  TAccountProvider extends string = string,
  TAccountVar extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEntropyProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  board: Address<TAccountBoard>;
  config: Address<TAccountConfig>;
  provider: Address<TAccountProvider>;
  var: Address<TAccountVar>;
  systemProgram?: Address<TAccountSystemProgram>;
  entropyProgram: Address<TAccountEntropyProgram>;
  id: NewVarInstructionDataArgs["id"];
  commit: NewVarInstructionDataArgs["commit"];
  samples: NewVarInstructionDataArgs["samples"];
};

export function getNewVarInstruction<
  TAccountSigner extends string,
  TAccountBoard extends string,
  TAccountConfig extends string,
  TAccountProvider extends string,
  TAccountVar extends string,
  TAccountSystemProgram extends string,
  TAccountEntropyProgram extends string,
  TProgramAddress extends Address = typeof ORE_PROGRAM_ADDRESS,
>(
  input: NewVarInput<
    TAccountSigner,
    TAccountBoard,
    TAccountConfig,
    TAccountProvider,
    TAccountVar,
    TAccountSystemProgram,
    TAccountEntropyProgram
  >,
  config?: { programAddress?: TProgramAddress },
): NewVarInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountBoard,
  TAccountConfig,
  TAccountProvider,
  TAccountVar,
  TAccountSystemProgram,
  TAccountEntropyProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    provider: { value: input.provider ?? null, isWritable: false },
    var: { value: input.var ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    entropyProgram: { value: input.entropyProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.board),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.provider),
      getAccountMeta(accounts.var),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.entropyProgram),
    ],
    data: getNewVarInstructionDataEncoder().encode(
      args as NewVarInstructionDataArgs,
    ),
    programAddress,
  } as NewVarInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountBoard,
    TAccountConfig,
    TAccountProvider,
    TAccountVar,
    TAccountSystemProgram,
    TAccountEntropyProgram
  >);
}

export type ParsedNewVarInstruction<
  TProgram extends string = typeof ORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    board: TAccountMetas[1];
    config: TAccountMetas[2];
    provider: TAccountMetas[3];
    var: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
    entropyProgram: TAccountMetas[6];
  };
  data: NewVarInstructionData;
};

export function parseNewVarInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedNewVarInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      board: getNextAccount(),
      config: getNextAccount(),
      provider: getNextAccount(),
      var: getNextAccount(),
      systemProgram: getNextAccount(),
      entropyProgram: getNextAccount(),
    },
    data: getNewVarInstructionDataDecoder().decode(instruction.data),
  };
}
